using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "TankPart", menuName = "Tank Parts/New Armor", order = 1)]
public class ArmorData : TankPartData
{
    public bool isSloped;
    // Start is called before the first frame update

}
